OPENAI_API_KEY=""
LANGSMITH_API_KEY=""
LANGSMITH_PROJECT=""
LANGCHAIN_TRACING_V2=true
LANGFUSE_SECRET_KEY=""
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_HOST="http://langfuse:3000"

# Job Application Tracking - Choose Supabase (recommended) or Google Sheets
USE_SUPABASE=true  # Set to false to use Google Sheets instead

# Supabase Integration (for job application tracking)
# 1. Create a Supabase account at https://supabase.com
# 2. Create a new project
# 3. Get your URL and keys from Settings > API
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_KEY="your-service-role-key"
# Optional: Database password for running migrations with psycopg2
SUPABASE_DB_PASSWORD=""

# Google Sheets Integration (legacy fallback)
# 1. Get your Sheet ID from the URL: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
GOOGLE_SHEETS_ID=""
# 2. Convert credentials.json to single line: python3 -c "import json; print(json.dumps(json.load(open('google-credentials.json'))))"
# 3. Paste the output below (use single quotes to wrap the JSON)
GOOGLE_SHEETS_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project",...}'
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USERNAME="my-email@gmail.com"
SMTP_PASSWORD="my app password" # must be an App Password, not your regular password
SMTP_FROM_EMAIL="my-email@gmail.com"
RUN_SEEDERS=true # note this isnt present in prod yet because we probably have to change it per \
# PC-12 https://williamroyco.atlassian.net/browse/PC-12
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pina_colada

# AUTH0
AUTH0_DOMAIN="dev-gzizexcaww2ggsh4.us.auth0.com"
AUTH0_AUDIENCE="http://localhost:8000"

# Verbose error logging (development only)
VERBOSE_ERROR_LOGS=true

# Provider Admin API Keys (for cost tracking)
# OpenAI: Get from https://platform.openai.com/settings/organization/admin-keys
OPENAI_ADMIN_KEY=""
# Anthropic: Get from Claude Console (requires admin role)
ANTHROPIC_ADMIN_KEY=""
